{
  "name": "HG BOARDS™",
  "version": "1.0.0",
  "description": "HG BOARDS™",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.0.0",
    "express": "^4.18.0",
    "express-session": "^1.17.0",
    "mongoose": "^8.0.0"
  }
}
const PORT = process.env.PORT || 3000;
app.listen(PORT, '0.0.0.0', () => console.log(`Active on port ${PORT}`));
